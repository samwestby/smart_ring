<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bluetooth_alarm">
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.bluetooth_alarm.SplashScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.bluetooth_alarm.DeviceList"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name="com.example.bluetooth_alarm.ledControl"
            android:label="LED Control">
        </activity>
        <activity
            android:name="com.example.bluetooth_alarm.AboutActivity"
            android:label="@string/title_activity_about">
        </activity>
    </application>
</manifest>
